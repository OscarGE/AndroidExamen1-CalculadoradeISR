package com.example.calculadoradeisr

import java.io.Serializable
/*Eseta clase es para crear un objeto con la información que se le
  pide al usario para hacer el cálculo de ISR. */
class Calculos (val periodo:String, val sueldo: Double): Serializable